Actúa como un Ingeniero de Software Experto en Prisma, PostgreSQL, TypeScript y Next.js. Necesito refactorizar mi esquema actual de Prisma y el código asociado (backend/frontend) para transformar el sistema en una plataforma Multi-Tenant y Multirrubro (que maneje variantes dinámicas con JSONB para soportar ropa, electrónica, materiales de construcción, etc.).

Por favor, guía la refactorización paso a paso, deteniéndote después de cada etapa para que pueda verificar los archivos antes de continuar. Mantén todos los IDs como `String @id @default(uuid())`.

---

### ETAPA 1: Esquema de Prisma (Multi-Tenant y Estructura Híbrida)
Modifica el archivo `schema.prisma`. Debes aplicar los siguientes cambios estructurales:
1. Crea el modelo `Tenant` para soportar múltiples clientes.
2. Agrega `tenantId String` y su relación correspondiente a TODAS las tablas existentes y nuevas (`Product`, `Stock`, `StockMovement`, `Sale`, `SaleItem`, `Branch`, `User`).
3. Actualiza los índices `@@unique` para que incluyan el `tenantId` (ej: un SKU es único por Tenant, un stock es único por variante/sucursal/tenant).
4. Implementa la relación Producto-Variante:
   - Mueve `sku`, `price`, y `cost` de `Product` hacia un nuevo modelo `Variant`.
   - Agrega `baseAttributes Json? @default("{}")` en `Product` (datos compartidos).
   - Agrega `attributes Json @default("{}")` en `Variant` (datos específicos del rubro).
   - Redirige las relaciones de `Stock`, `StockMovement` y `SaleItem` para que apunten a `Variant` en lugar de a `Product`.

Muéstrame el código completo del nuevo `schema.prisma` y espérame.

---

### ETAPA 2: Migración de Base de Datos y Tipos de TypeScript
Una vez aprobado el esquema:
1. Genera los comandos necesarios para crear la migración de Prisma.
2. Si el agente detecta que la base de datos actual tiene datos, escribe un script de migración temporal en SQL para mover el `sku`, `price` y `cost` de los productos actuales hacia una variante por defecto para no perder información.
3. Actualiza las definiciones de tipos de TypeScript generadas por Prisma en el backend para reflejar los campos JSONB (`baseAttributes` y `attributes`).

Muéstrame los pasos y archivos a modificar, y espérame.

---

### ETAPA 3: Backend (Servicios, Repositorios y Rutas de Next.js)
Refactoriza la lógica del backend en Next.js considerando que ahora todo se filtra por `tenantId` (que vendrá en el contexto/sesión del usuario):
1. Modifica las consultas de creación de productos para que permitan guardar el JSON de `baseAttributes` y crear sus `Variant` en un solo `connectOrCreate` o `create` anidado.
2. Modifica el listado de productos para el POS/Ventas: debe buscar variantes directamente cruzando el JSON del producto y la variante. Utiliza el operador de fusión de objetos de JavaScript/TS o query raw de Prisma si es necesario para simular el operador `||` de Postgres.
3. Actualiza los controladores de venta (`Sale` y `SaleItem`) y movimientos de stock (`StockMovement`) para que trabajen con `variantId` en lugar de `productId`.

Presenta los cambios divididos por archivos de API/Rutas y espérame.

---

### ETAPA 4: Frontend (Vite + React + TailwindCSS)
Modifica los componentes del Frontend para adaptarse a la nueva interfaz:
1. Actualiza los formularios de creación de productos para que el usuario pueda definir características comunes del rubro (ej: rendimiento por caja de cerámica, talle de ropa) y generar múltiples variantes de forma dinámica.
2. Actualiza la pantalla del Punto de Venta (POS): al escanear o buscar para vender, la interfaz debe listar e identificar filas de `Variant` (filtrando por su SKU/Atributo específico).
3. Modifica el catálogo online (si aplica) para agrupar las variantes bajo el modelo visual de un solo `Product`, desplegando selectores dinámicos basados en las llaves del JSON de sus variantes.

Indícame qué archivos del frontend se deben modificar y el código correspondiente.
