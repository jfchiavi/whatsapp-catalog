Actúa como un Ingeniero de Software Experto en Prisma, PostgreSQL, TypeScript y Next.js. Necesito refactorizar mi esquema actual de Prisma y el código asociado (backend/frontend) para transformar el sistema en una plataforma Multi-Tenant y Multirrubro (que maneje variantes dinámicas con JSONB para soportar ropa, electrónica, materiales de construcción, etc.).

Por favor, guía la refactorización paso a paso, deteniéndote después de cada etapa para que pueda verificar los archivos antes de continuar. Mantén todos los IDs como `String @id @default(uuid())`.

---

### ETAPA 1: Esquema de Prisma (Multi-Tenant y Estructura Híbrida)
Modifica el archivo `schema.prisma`. Debes aplicar los siguientes cambios estructurales:
1. Crea el modelo `Tenant` para soportar múltiples clientes.
2. Agrega `tenantId String` y su relación correspondiente a TODAS las tablas existentes y nuevas (`Product`, `Stock`, `StockMovement`, `Sale`, `SaleItem`, `Branch`, `User`).
3. Actualiza los índices `@@unique` para que incluyan el `tenantId` (ej: un SKU es único por Tenant, un stock es único por variante/sucursal/tenant).
4. Implementa la relación Producto-Variante:
   - Mueve `sku`, `price`, y `cost` de `Product` hacia un nuevo modelo `Variant`.
   - Agrega `baseAttributes Json? @default("{}")` en `Product` (datos compartidos).
   - Agrega `attributes Json @default("{}")` en `Variant` (datos específicos del rubro).
   - Redirige las relaciones de `Stock`, `StockMovement` y `SaleItem` para que apunten a `Variant` en lugar de a `Product`.

Muéstrame el código completo del nuevo `schema.prisma` y espérame.

---

### ETAPA 2: Migración de Base de Datos y Tipos de TypeScript
Una vez aprobado el esquema:
1. Genera los comandos necesarios para crear la migración de Prisma.
2. Si el agente detecta que la base de datos actual tiene datos, escribe un script de migración temporal en SQL para mover el `sku`, `price` y `cost` de los productos actuales hacia una variante por defecto para no perder información.
3. Actualiza las definiciones de tipos de TypeScript generadas por Prisma en el backend para reflejar los campos JSONB (`baseAttributes` y `attributes`).

Muéstrame los pasos y archivos a modificar, y espérame.

---

### ETAPA 3: Backend (Servicios, Repositorios y Rutas de Next.js)
Refactoriza la lógica del backend en Next.js considerando que ahora todo se filtra por `tenantId` (que vendrá en el contexto/sesión del usuario):
1. Modifica las consultas de creación de productos para que permitan guardar el JSON de `baseAttributes` y crear sus `Variant` en un solo `connectOrCreate` o `create` anidado.
2. Modifica el listado de productos para el POS/Ventas: debe buscar variantes directamente cruzando el JSON del producto y la variante. Utiliza el operador de fusión de objetos de JavaScript/TS o query raw de Prisma si es necesario para simular el operador `||` de Postgres.
3. Actualiza los controladores de venta (`Sale` y `SaleItem`) y movimientos de stock (`StockMovement`) para que trabajen con `variantId` en lugar de `productId`.

Presenta los cambios divididos por archivos de API/Rutas y espérame.

---

### ETAPA 4: Frontend (Vite + React + TailwindCSS)
Modifica los componentes del Frontend para adaptarse a la nueva interfaz:
1. Actualiza los formularios de creación de productos para que el usuario pueda definir características comunes del rubro (ej: rendimiento por caja de cerámica, talle de ropa) y generar múltiples variantes de forma dinámica.
2. Actualiza la pantalla del Punto de Venta (POS): al escanear o buscar para vender, la interfaz debe listar e identificar filas de `Variant` (filtrando por su SKU/Atributo específico).
3. Modifica el catálogo online (si aplica) para agrupar las variantes bajo el modelo visual de un solo `Product`, desplegando selectores dinámicos basados en las llaves del JSON de sus variantes.

Indícame qué archivos del frontend se deben modificar y el código correspondiente.


--------------------------------------
# PROMPT Actualizado 27/05/2026

Actúa como un Ingeniero de Software Principal experto en Prisma, PostgreSQL, TypeScript, Zod y Next.js. Vamos a refactorizar mi proyecto actual (Vite + React + Tailwind en front; Next.js + Prisma en backend). El sistema ya está avanzado (90% backend, 75% frontend), por lo que debemos realizar modificaciones quirúrgicas sin romper las transacciones atómicas, middlewares (RBAC) ni hooks existentes.

Objetivo principal: Transformar el sistema en Multi-Tenant, implementar el sistema de variantes dinámicas con JSONB para soportar múltiples rubros (construcción, ropa, electrónica), añadir los campos faltantes del reporte de desarrollo y solucionar bugs críticos.

Por favor, ejecuta este plan paso a paso. Detén tu ejecución al finalizar cada etapa y espérame para validar. Conserva siempre los IDs como String @id @default(uuid()).

---

### ETAPA 1: Modificación del Schema de Prisma (Multi-Tenant, Variantes JSONB y Datos Faltantes)
Modifica el archivo `schema.prisma` para aplicar los siguientes cambios exactos, manteniendo la compatibilidad con lo existente:

1. MODELO TENANT: Crea el modelo `Tenant`. Agrega la columna `tenantId String` y su relación a TODOS los modelos (`User`, `Branch`, `Product`, `Variant`, `Stock`, `StockMovement`, `Sale`, `SaleItem`). Ajusta los índices `@@unique` para que incluyan `tenantId` (ej. el SKU o el par Product/Branch ahora son únicos POR tenant).
2. MODELO BRANCH (Mejoras): Agrega los campos opcionales `address String?` y `hours String?`.
3. MODELO PRODUCT (Híbrido + Campos faltantes del reporte):
   - Mueve `sku`, `price` y `cost` hacia el nuevo modelo `Variant`.
   - Agrega `imageUrl String?`, `batch String?`, y `expirationDate DateTime?`.
   - Agrega `baseAttributes Json? @default("{}")` para almacenar datos técnicos compartidos del rubro (ej: metros por caja de cerámica).
4. MODELO VARIANT (Nuevo con JSONB): Crea el modelo `Variant` con `id`, `productId`, `tenantId`, `sku`, `price Float`, `cost Float` y `attributes Json @default("{}")` (para datos específicos como color, talle o diseño técnico).
5. RELACIONES DE STOCK Y VENTAS: Redirige `Stock`, `StockMovement` y `SaleItem` para que apunten a `Variant` en lugar de a `Product`. En `StockMovement`, agrega `userId String?` para registrar quién ejecutó el movimiento.

Muéstrame el archivo `schema.prisma` finalizado y espera mi confirmación.

---

### ETAPA 2: Scripts de Migración de Datos y Actualización de Tipos TS
Para no perder la información actual en la base de datos debido al movimiento de columnas:
1. Escribe un script de migración en SQL o una función en TypeScript que tome los valores actuales de `sku`, `price` y `cost` de la tabla `Product`, cree una `Variant` por defecto para cada producto y mueva los registros de stock hacia esa nueva variante.
2. Actualiza los tipos de TypeScript generadas por Prisma en el backend, definiendo interfaces estrictas para los campos `Json` (ej: un tipo general para atributos de construcción, otro para ropa, etc.).

Presenta los archivos/scripts correspondientes y espera mi confirmación.

---

### ETAPA 3: Backend - Fix de Ventas, Validaciones Zod y Nuevos Endpoints
Refactoriza el backend de Next.js aplicando estas prioridades del reporte:
1. FIX BUG SUPER_ADMIN (route.ts:53): Corrige la lógica de creación de ventas. Si el usuario es un SUPER_ADMIN y su `branchId` es null, el sistema debe exigir que se envíe explícitamente un `branchId` válido en el cuerpo de la petición de venta, o rechazarla con un error claro si no se especifica.
2. VALIDACIONES ZOD: Actualiza los esquemas de Zod para la creación de productos y ventas para incluir la estructura de las variantes, `baseAttributes`, `attributes` e `imageUrl`.
3. TRANSACCIÓN DE VENTA CON VALIDACIÓN DE STOCK: Ajusta el endpoint `POST /api/sales`. Antes de ejecutar la transacción atómica, debe verificar si hay stock suficiente de la `Variant` específica en la sucursal seleccionada. Si no hay, debe arrojar un error HTTP 400 antes de decrementar.
4. ENDPOINTS PENDIENTES: 
   - Crea `GET /api/stock/branch/:branchId` para listar el stock de esa sucursal.
   - Crea `GET /api/stock/history/:variantId` (o productId resolviendo sus variantes) para mapear el historial del frontend.

Presenta los archivos modificados agrupados de a uno y espera mi confirmación.

---

### ETAPA 4: Frontend - Componentes de Variantes, Integración de Selectores y Validaciones
Refactoriza la interfaz de Vite + React + Tailwind:
1. HOOKS DE REACT QUERY: Modifica `useProducts`, `useSales` y `useStock` para mapear las llamadas hacia las variantes en lugar de los productos directos, inyectando el `tenantId` correspondiente desde el contexto de autenticación en las cabeceras.
2. SELECTOR DE VENTAS (CreateSalePage): Modifica el carrito y el selector de productos para que, al elegir un producto, se desplieguen dinámicamente sus variantes disponibles (basadas en las llaves del JSON). Bloquea el botón de añadir si el stock disponible de la variante en la sucursal actual es 0.
3. MODAL DE CREACIÓN DE PRODUCTOS (CreateProductModal): Rediseña el formulario para permitir cargar la imagen (`imageUrl`), lote y vencimiento. Añade un apartado dinámico donde el usuario elija el rubro y pueda generar múltiples variantes (especificando sus atributos JSONB individuales) antes de enviar el formulario.

Dime qué archivos del frontend se van a modificar y comencemos con el primero.
